{% layout 'layout.liquid' %}
{% block content %}
  <div>
    <h2 class="display-6 ms-3 mt-3">Hello {{ username }} !                                        </h2>
    <a href="/auth/logout" class="btn btn-danger ms-3">Log Out</a>
    <hr>
    <div class="row">
      <!-- <a href="/pokemon/new" class="btn btn-primary me-3 ms-3 mb-3" style="display: inline; width: 300px;">New Pokemon</a>-->
      <a href="/pokemon/mine" class="btn btn-primary me-3 ms-3 mb-3" style="display: inline; width: 300px; ">Your Pokemons</a><hr> 
      <!--
        <h3 class="ms-3">Your Favorite Pokemons</h3> 
      {% for pokemon in pokemons %}
        <div class="card ms-3" style="width: 18rem;">
          <h4 class="card-header" style="text-transform: capitalize;">{{ pokemon.name }}</h4>
          <div class="card-body">
            <p class="card-text">
              Type: {{ pokemon.type1 }} {{ pokemon.type2 }}
            </p>
            <a href="/pokemon/{{ pokemon.id }}" class="btn btn-info">
              Info
            </a>
            <a href="/pokemon/{{ pokemon.id }}/edit" class="btn btn-success">
              Edit
            </a>
          </div>
        </div>
      {% endfor %}
-->
      <h3 class="ms-3">Your Teams</h3>
      {% for team in teams %}
        <div class="card ms-3" style="width: 14rem;">
          <h4 class="card-header">Team "{{ team.name }}"</h4>
          <div class="card-body">
          <h6>{{ team.description }}</h6>
          {% for pokemon in team.pokemons %}
            <img src="{{ pokemon.sprite }}" alt="">
            {% endfor %}
            <a href="/team/{{ team.id }}" class="btn btn-info">
              View {{ team.name }}
            </a>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
{% endblock %}