{% layout 'layout.liquid' %}
{% block content %}
<div class="container-sm">
    <article>
        <h1 class="mt-3 " style="text-transform: capitalize;"> <img class =" {{ pokemonData.types.0.type.name }}"style="border-width: 3px; border-radius:20px;"src="{{ pokemonData.sprites.front_default }}" alt="no-sprite"> <i>{{ pokemonData.id}} -</i>
             {{ pokemonData.name}}
            </h1> 
        <img src="{{ pokemonData.sprites.other.official-artwork.front_default }}" alt="artwork">
        <h3 style="text-transform: capitalize">Type: {{ pokemonData.types.0.type.name }}  {{ pokemonData.types.1.type.name }}</h3>
        {% if pokemon.team %}
        <a href="/team/{{ pokemon.team }}" class="btn btn-primary" style="text-transform: capitalize">View {{ pokemonData.name }}'s Team</a>
        {% endif %}
        {% if pokemon.owner == userId %}
        <div class="btn-group">
            <form action="/pokemon/{{ teamId }}/{{ pokemon.id }}?_method=DELETE" method="POST">
                <input type="submit" value="Delete Pokemon" class="btn btn-danger">
            </form>
        </div>
        {% endif %}
    </article>
    
    <small>Trained by: 
        {% if pokemon.owner == userId %}
        You ☆
        {% else  %}
        {{ pokemon.owner.username }} 
       {% endif %}
        </small><br>
    <small>PKMN id: {{ pokemon.id }}</small>
</div>
{% endblock %}